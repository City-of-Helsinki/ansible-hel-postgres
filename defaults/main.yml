postgres_install_engine: yes
postgres_local_db: true

postgres_create_db: true
postgres_destroy_db: no
postgres_postgis: no
postgres_hstore: no

postgres_preload_data: false

postgres_wanted_extensions: []

#postgres_source_pghost: "{{ postgres_host }}"
#postgres_source_pglogin: "{{ postgres_login }}"
#postgres_source_pgpw: "{{ postgres_login }}"